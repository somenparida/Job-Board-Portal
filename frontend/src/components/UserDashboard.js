import React from 'react';

export default function UserDashboard() {
  return (
    <div>
      <h3>User Dashboard</h3>
      <p>Welcome — browse available jobs and apply (apply flow not implemented in scaffold).</p>
    </div>
  );
}
